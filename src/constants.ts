export enum ROUTES {
  Home = "/translator/",
  NewWords = "/translator/new-words",
  Test = "/translator/test",
  Results = "/translator/results",
}

export enum STORAGENAME {
  WORDSLIST = "WORDSLIST",
  RESULTS = "RESULTS",
}
